version: '3.8'

services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: afyia-website
    ports:
      - "4200:80"
    volumes:
      - ./dist/afyia-website/browser:/usr/share/nginx/html
